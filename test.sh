#!/bin/sh 
      # app_name="sayil"
      # mkdir $app_name 
      # cd $app_name 
      # npm init -y
      # data='[express,cors,body-parser,Mongo,moment-business]'
      # data=${data// /}
      # data=${data//,/ }
      # data=${data##[}
      # data=${data%]}
      # eval data=($data)
      # for pkgs in "${data[@]}"
      # do 
      #  echo ${pkgs}
      # done
           
     data="sayil"
     echo \"${data} \"